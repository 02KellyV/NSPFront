

export const sources = {
    set: '@sources/set',
    setFile: '@sources/setFile',
    selected: '@sources/selected',
    download: '@sources/download',
}

export const auth = {
    setSession: '@auth/setSession'
}